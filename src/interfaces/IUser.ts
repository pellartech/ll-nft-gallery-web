export type IUser = {
    name?: string,
    bio?: string,
    wallet_address?: string,
    twitter?: string,
    instagram?: string,
    discord?: string,
    avatar?: {
        original?: string
    }
}
